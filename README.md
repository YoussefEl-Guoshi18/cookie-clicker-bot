# 🍪 Cookie Clicker Bot

A Python automation script that plays **Cookie Clicker** using Selenium.
The bot clicks the cookie continuously and automatically purchases the
most expensive available upgrade at regular intervals.

## 🚀 Features
- Automatic cookie clicking
- Smart upgrade purchasing
- Runs for a fixed time period
- Uses Selenium WebDriver

## 🧰 Requirements
- Python 3.8+
- Google Chrome
- ChromeDriver (matching your Chrome version)

## 🔧 Setup

1. Install dependencies:
```bash
pip install -r requirements.txt
